# 京东企业级电商项目 AI 规则

本文件是本仓库内所有 AI 工具协作的唯一规则源。其他工具专属规则文件只能指向这里，不能各自重新定义一套规则。

## 项目结构

- `frontend/` 是面向用户的移动端电商应用。
- `admin/` 是独立的运营后台应用。不要把后台路由合并进 `frontend/`。
- `backend/` 是使用 Maven 构建的 Spring Boot 后端。
- 面向用户的解释默认使用中文，除非用户明确要求使用其他语言。
- 所有新增或更新的项目文档、方案、规范和说明默认使用中文正文；路径、命令、代码标识、协议字段和必要英文技术名词可以保留原文。

## 强制规则

- 前端和后台的依赖、脚本、构建统一使用 `pnpm`。禁止使用 `npm install`、`npm run` 或生成 npm lockfile。
- 后端使用 Spring Boot + Maven + MyBatis-Plus。禁止使用 JPA。
- 除非用户明确推翻当前决定，否则不要引入 Flyway。需要兼容存量表结构时，继续使用轻量启动迁移逻辑。
- 鉴权使用 Spring Security + 轻量 JWT/RBAC 方案。除非用户明确要求，不要引入 session、表单登录、HTTP Basic、重型 OAuth2/SAML 体系，也不要改回 MVC 拦截器鉴权。
- 除非用户明确要求接入真实支付网关，否则支付继续模拟成功。
- Redis 只用于 JWT 黑名单、限流等安全或运行时状态。除非用户明确要求，不要缓存首页、商家详情页或商品详情数据。
- 除非用户要求，不要为后端创建 Docker 打包方案；后端默认在本地运行测试。
- 必须保护用户已有改动。未经明确授权，不要执行破坏性 git 命令，也不要回滚无关文件。

## 后端要求

- 接口基础路径是 `/api`；后台接口统一位于 `/admin/**`。
- 后台访问仅允许 `ADMIN` 和 `OPERATOR`；`CUSTOMER` 必须返回 `403`。
- 密码必须使用 PBKDF2 哈希。历史明文密码可以在成功登录后自动升级为哈希。
- 订单主流程为 `PAID -> PREPARING -> DELIVERING -> COMPLETED`，异常流程包括 `CANCELED`、`REFUND_REQUESTED`、`REFUNDED`。
- 创建订单必须扣减库存。取消订单或确认退款时必须且只能回滚一次库存。
- 关键操作应写入审计日志，并在日志和异常中关联 request ID。
- MySQL、Redis、JWT 配置必须支持环境变量覆盖。

## 前端要求

- `frontend/` 保持面向用户、移动端优先。
- `admin/` 保持面向运营后台、桌面端优先，并使用 Element Plus。
- 做联调或真实功能时，两个前端项目默认调用真实后端接口；不要静默替换为假数据。
- 如果接口结构变化，必须同步更新 TypeScript 领域类型和 service/API 函数。

## 完成前验证

根据改动范围运行对应检查，并在回复中报告真实结果：

- 后端：`cd backend && mvn clean test`
- 用户端：`cd frontend && pnpm build`
- 后台端：`cd admin && pnpm build`
- 修改鉴权、后台、订单相关行为时：启动后端并做 HTTP 冒烟，至少验证登录、后台访问、普通用户 `403`、被修改接口的行为。

如果命令失败，必须停止并报告准确失败信息。不要根据猜测声明完成。

## 推荐工作流

- 修改前先阅读现有代码和配置。
- 非平凡功能应新增或更新测试覆盖行为。
- 不要把生成的构建产物混入源码管理。
- 如果当前 AI 工具支持 skill、plugin 或工作流能力，应使用其规划、调试、验证流程，而不是临场 improvising。
- 涉及风控、安全、支付验签、退款治理、后台高危操作或隐私合规时，若当前 AI 工具支持 skill，应优先使用 `ecommerce-risk-security`；不支持 skill 的工具也必须按同类检查清单进行系统性审查。
